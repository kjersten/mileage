<%= form_for(@fillup) do |f| %>
  <% if @fillup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fillup.errors.count, "error") %> prohibited this fillup from being saved:</h2>

      <ul>
      <% @fillup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :date %>
    <%= f.text_field :date, :value => "#{@fillup.date.strftime '%m/%d/%Y'}", :class => "datepicker" %>
  </div>
  <div class="field">
    <%= f.label :gallons %>
    <%= f.text_field :gallons %>
  </div>
  <div class="field">
    <%= f.label :miles %>
    <%= f.text_field :miles %>
  </div>
  <div class="field">
    <%= f.label :price_per_gallon %>
    <%= f.text_field :price_per_gallon %>
  </div>
  <div class="field">
    <%= f.label :note %>
    <%= f.text_field :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
